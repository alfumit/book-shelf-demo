import { YearValidationDirective } from './year-validation.directive';

describe('MinDirective', () => {
  it('should create an instance', () => {
    const directive = new YearValidationDirective();
    expect(directive).toBeTruthy();
  });
});
